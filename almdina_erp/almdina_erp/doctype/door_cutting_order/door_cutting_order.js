(() => {
    "use strict";

    // Door Cutting Order behaviour is composed from focused modules in hooks.py.
    // Keep this canonical Frappe entry point side-effect free so framework
    // upgrades can still resolve it without restoring the legacy form monolith.
})();
